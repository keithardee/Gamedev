PORT=5000
GANACHE_RPC_URL=http://127.0.0.1:7545
CHAIN_ID=1337
# Leave empty to use virtual developer wallet mode (event-ledger only).
DEV_WALLET=
# Optional: only show purchases that came from this wallet address.
SOURCE_WALLET=
CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
BLOCK_LOOKBACK=500
# Optional: JSON array string for custom contract event ABI fragments.
# Example: ["event CoinsPurchased(address indexed player, uint256 coinAmount, uint256 paidWei)"]
EVENT_ABI_JSON=[]
MONITORING_MODE=deductions
DEVELOPER_SHARE_RATE=1
